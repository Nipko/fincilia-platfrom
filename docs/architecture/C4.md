# C4 v0 — semilla de trabajo

- Estado: Draftable
- Tarea: FNC-ARC-001
- Gate: S1-READY
- Owner: UNASSIGNED
- Referencias: plan §20–§26

Este documento es una semilla revisable, no arquitectura aceptada. A-01 y A-02 permanecen abiertos.

## Contexto

~~~mermaid
flowchart LR
  Firm[Firma contable] --> F[Fincilia]
  SME[PYME cliente] --> F
  Auditor[Auditor o revisor] --> F
  F --> IdP[IdP B2B]
  ERP[ERP / proveedor de facturación] --> F
  Bank[Bancos y agregadores] --> F
  Pay[Pasarelas y marketplaces] --> F
  Mail[Correo / SFTP / archivos] --> F
  F --> Notify[Email / push]
  F --> OCR[OCR o IA autorizada]
~~~

Fincilia no es ERP, banco, iniciador de pagos ni proveedor tecnológico DIAN en el alcance inicial.

## Contenedores

~~~mermaid
flowchart TB
  Web[Web] --> Edge[CDN WAF API Gateway]
  Mobile[Mobile companion] --> Edge
  Sources[Upload correo SFTP API] --> Edge
  Edge --> App[Monolito modular]
  App --> PG[(PostgreSQL)]
  App --> Obj[(Object storage)]
  App --> Outbox[Transactional outbox]
  Outbox --> Queue[Queue]
  App --> Temporal[Temporal]
  Queue --> Workers[Workers aislados]
  Temporal --> Workers
  Workers --> Obj
  Workers --> App
  App --> Valkey[(Valkey efímero)]
  App --> AIG[AI Gateway]
  AIG --> Providers[OCR IA autorizada]
  App --> OTel[OpenTelemetry]
  OTel --> Sec[Cuenta seguridad logs]
  PG --> Backup[Backup separado]
  Obj --> Backup
~~~

Fuentes autoritativas:

- PostgreSQL: dominio y estado visible.
- Object storage: evidencia binaria y versiones.
- Temporal: ejecución durable.
- Valkey: progreso/caché, nunca verdad.
- Cuenta de seguridad: auditoría, delete ledger y logs de nube.

## Componentes del monolito

~~~mermaid
flowchart LR
  IAM[IAM Access] --> Tenancy[Organization Company Engagement]
  Tenancy --> Sources[Sources]
  Sources --> Ingestion[Ingestion]
  Ingestion --> Clean[Clean]
  Clean --> Finance[Finance]
  Finance --> Recon[Reconciliation]
  Recon --> Close[Close]
  Close --> Reporting[Reporting]
  Finance --> Risk[Risk]
  All[Todos los módulos] --> Audit[Audit]
  Reporting --> Usage[Usage Billing]
  Ingestion --> Platform[Jobs Outbox Releases]
  Recon --> Platform
~~~

Las flechas expresan dependencia permitida, no acceso directo a tablas. MODULE_BOUNDARIES.md define ownership.

## Decisiones abiertas

- FNC-PLT-001: NestJS/TypeScript frente a alternativa coherente.
- ADR-002: versión exacta de PostgreSQL y tooling SQL.
- A-02: región y proveedor productivo.
- IdP B2B.
- Cola concreta y Temporal gestionado.

